databaseChangeLog:
  - changeSet:
      id: 01
      author: kaique.cidrini
      context: local
      preConditions:
        - onFail: MARK_RAN
        - sqlCheck:
            expectedResult: 0
            sql: SELECT COUNT(1) FROM pg_roles WHERE rolname = 'mediaverse_user';
      changes:
        - sql:
            comment: Create mediaverse_user role
            endDelimiter: ;
            splitStatements: true
            sql: CREATE ROLE mediaverse_user WITH SUPERUSER CREATEDB CREATEROLE LOGIN ENCRYPTED PASSWORD 'Postgres123';
